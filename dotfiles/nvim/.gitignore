tags
test.sh
.luarc.json
nvim

spell/

# NIXOS: upstream ignores this to avoid merge conflicts; we TRACK it, because
# pinning plugin versions is the whole point of a reproducible config.
# See :help vim.pack-lockfile
# nvim-pack-lock.json

.DS_Store
